"""L0 kernel — ports (interfaces), DTOs, and the one loop.

Dependency rule: this package imports NOTHING internal (stdlib only). It must
never name a capability, a product, or the service. See
docs/one-loop-two-layer-agent.md §2 and docs/architecture.md §1.
"""
